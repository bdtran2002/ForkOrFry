# Listing assets placeholder

Store future Firefox Add-on listing assets here, for example:

- screenshots
- promo tiles
- icons used for store metadata
- AMO-safe branding drafts

Keep runtime extension icons in `extension/public/`.
