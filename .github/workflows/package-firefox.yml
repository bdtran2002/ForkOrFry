name: Package Firefox Extension

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

concurrency:
  group: package-firefox-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ForkOrFry
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: npm
          cache-dependency-path: ForkOrFry/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Package unsigned Firefox XPI
        run: npm run package:firefox

      - name: Upload unsigned Firefox XPI
        uses: actions/upload-artifact@v4
        with:
          name: forkorfry-firefox-xpi
          path: ForkOrFry/dist/forkorfry-firefox-mv3.xpi
          if-no-files-found: error
